# FINAL SESSION — Phase 4: figures, tables, claims

**Box:** 90 min. **Analysis only — no measurements.** Everything here is
regenerated by `tools/make_figures.py` (extended, not rewritten) from
CSVs on disk. Machine exclusivity: N/A.

`make_wp3.py` now ends with:
```
figure1(); figure2(); figure3(); figure4(); figure5(); figure6()   # original
table1(); table2()
figure6_final(); figure7(); table1_final(); table2_final()          # FINAL SESSION
```

---

## Figures (all in `results/overnight/figures/`, PNG @ 200 dpi + CSV)

| # | file | what changed this session | source |
|---|---|---|---|
| 1 | `figure1_bytes_per_work.{png,csv}` | unchanged structure; declared arm-D line still from `results/data/declared-vs-learned-policy.csv` (post-WP0-fix). **Note for the writeup:** Phase 2 showed `all-threads`+off reads ≤ this at 128 MiB (D/OPT 1.15→1.08); the synthetic Figure 1 is now conservative for arm D. | Campaign 12 Phase D (A/C) + WP1 (D/E) |
| 2 | `figure2_miss_rate.{png,csv}` | unchanged. | same |
| 3 | `figure3_chunk_size_tradeoff.{png,csv}` | unchanged (historical chunk-size sweeps). | Campaign 11 Phase 3 + Campaign 12 Phase B |
| 4 | `figure4_reclaim_authority.{png,csv}` | unchanged (spike S3d/S3e). | spike |
| 5 | `figure5_prefetch_total_fetches.{png,csv}` | unchanged. | Campaign 12 Phase D |
| **6** | `figure6_llamacpp.{png,csv}` | **regenerated from Phase 1's equal-budget sweep.** 5 ratios (was 3). Arm A = `memory.max = B` (was B+256 MiB). Arm E at r=0.25 = the Phase 3 fallback config (protect-off + retention-none), drawn as a ★ with a note (the default arm-E config deadlocks there). OPT from `results/data/real-model-opt-bound.csv` (65 passes). | `results/data/real-model-bytes-by-budget.csv`, `results/data/real-model-opt-bound.csv`, `results/data/prefetch-collapse-fallback-configs.csv` |
| **7** | `figure7_throughput_scaling.{png,csv}` | **NEW.** tokens/s vs budget ratio, arms A/C/D/E, real model, equal budget. Arms A and C horizontal (0.6–0.9 t/s at every budget); arm D rises 0.91 → 1.95 t/s (2.1×). This is Claim 9's figure. | `results/data/real-model-bytes-by-budget.csv` |

### What Figure 6 shows (equal budget)

- **Arm D beats arm A on bytes at every ratio** — D/A = 0.98 / 0.89 / 0.71 /
  0.58 / 0.52. r=0.25 is parity (within the disclosed asymmetry); clear D win at
  r ≥ 0.375. WP2's "D loses at r=0.25" was the +256 MiB confound.
- **D/OPT = 1.14 / 1.09 / 1.09 / 1.10 / 1.13** — within 14 % of Belady at every
  ratio on a real model.
- **Arm C flat-thrashes** 134–144 GB at every budget.
- **Right panel = Figure 7's story**: A and C flat, D and E rising.

---

## Tables (in `results/overnight/figures/`)

| # | file | contents |
|---|---|---|
| 1 (orig) | `table1_main_results.{md,csv}` | synthetic grid, unchanged. |
| 2 (orig) | `table2_environment.{md,csv}` | synthetic env, unchanged (still says "WP2 not run" — see the FINAL companion). |
| **1 (FINAL)** | `table1_final_real_model.{md,csv}` | **real model, equal budget.** arm × ratio → read GB, read/OPT, demand faults, tokens/s, p99 inter-token. Arm E's r=0.25 row is the Phase 3 fallback with a `COLLAPSES in default config` note. |
| **2 (FINAL)** | `table2_final_environment.{md,csv}` | **live-queried environment** + the real model sha256 (`626b4a66…62d`), the equal-budget setup and its residual asymmetry, the final default policy (`all-threads` + protect-off), the r ≤ 0.375 prefetch-off recommendation, measured per-layer compute (~51 k ns/MiB), T-1..T-7 status. |

---

## Claims — `results/claims.md`

A **"FINAL SESSION amendments"** block was added at the top (read first), and
**Claim 9** was added in full:

> **Claim 9:** The kernel cannot convert additional memory into throughput on a
> cyclic layer scan; an application-authoritative pager can. Evidence: Phase 1's
> tokens/s column (A/C flat at 0.6–0.9 t/s across all 5 ratios; D rises 2.1×),
> Figure 7. Strength: strong (direction); arm A's absolute values are
> reclaim-noisy.

Amendment summary:
- **Claims 2, 4, 7** — the compute=400000 non-determinism and the 128 MiB D/OPT
  regression are **fixed** by Phase 2's exact all-threads signal (1.15 → 1.08).
- **Claims 7, 8** — "D ≈ A at r=0.25" **superseded** by Phase 1's equal budget:
  D beats A at every ratio.
- **Claim 7** — arm E's r=0.25 collapse **characterised** (Phase 3 deadlock);
  stated operating limit: no prefetch benefit at r ≤ 0.375.
- **Claim 6** — confirmed on the real model at equal budget (E costs 7–13 %
  bytes, wins ~2× on faults; no "E beats D on bytes" regime on this model).

Every claim's Caveats field states: WSL2-only, one 3B dense model, CPU-only,
host-cache non-contamination where checked, and the non-deterministic/excluded
cells.

---

## Final check

- Every figure has a PNG @ 200 dpi and a CSV of its numbers.
- Every number traces to a named CSV (`results/data/real-model-bytes-by-budget.csv`,
  `results/data/real-model-opt-bound.csv`, `phase2_*.csv`, `results/data/prefetch-collapse-fallback-configs.csv`) or a spike log — nothing
  re-measured or estimated for this work package.
- Arm A's Phase 1 points carry the achieved bandwidth (920–1310 MiB/s) — none
  exceed the 3396 MiB/s O_DIRECT ceiling, so **no host-cache-contamination flag
  on any real-model cell** (unlike the synthetic Figure 1).
- Arm E's r=0.25 collapse is marked on Figure 6 and Table 1, not omitted.
- `make_wp3.py` was extended (4 functions added, 1 call line changed); Figures
  3/4/5 and the original tables are untouched.
